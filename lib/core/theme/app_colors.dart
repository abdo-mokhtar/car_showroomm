import 'package:flutter/material.dart';

class AppColors {
  // ✅ Primary Colors
  static const Color primary = Color(0xFFE94560);
  static const Color secondary = Color(0xFF0F3460);
  static const Color accent = Color(0xFF16213E);

  // ✅ Background Colors
  static const Color background = Color(0xFF1A1A2E);
  static const Color surface = Color(0xFF16213E);
  static const Color card = Color(0xFF0F3460);

  // ✅ Text Colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.grey;
  static const Color textHint = Color(0xFF6B7280);

  // ✅ Status Colors
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF3B82F6);

  // ✅ Car Status
  static const Color available = Color(0xFF10B981);
  static const Color sold = Color(0xFFEF4444);

  // ✅ Payment Types
  static const Color cash = Color(0xFF10B981);
  static const Color transfer = Color(0xFF3B82F6);
  static const Color check = Color(0xFFF59E0B);
  static const Color installment = Color(0xFF8B5CF6);

  // ✅ Sidebar
  static const Color sidebarBg = Color(0xFF16213E);
  static const Color sidebarItem = Colors.white;
  static const Color sidebarIcon = Color(0xFFE94560);
}
